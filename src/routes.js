import React from 'react'
import { Router, Switch, Route } from 'react-router-dom'

export default function routes() {
  return (
    <Router>
      <Switch>
        <Route path="/floating" component={ }></Route>
      </Switch>

    </Router>
  )
}
